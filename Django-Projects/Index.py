import atom

check_list = [1,1000] def find_solution(step):
    for num in xrange(step,9999999,step)
        if all(num % n == 0 for n in check_list):
            return num

    return None if name == '  main__':
    solution = find_solution(1000)
    if solution is None:
        print "No Answer found"
    else:
        print "found an answer, solution"
